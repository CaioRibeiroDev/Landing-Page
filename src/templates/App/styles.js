import styled, { css } from 'styled-components';

export const Wrapper = styled.div``;

// este exemplo permite que você passe o "styledcomponents" por props
/*export const Wrapper = styled.div`
  ${(props) => css`
    background: ${props.background};
  `}
`;*/
